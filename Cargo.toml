[package]
name = "stdworkspace"
version = "0.1.0"
edition = "2021"

[workspace]
members = [ "adder",
    "chapters", 
    "collections", "concurrency", "drop", "functionallaguage", "minigrep", "oop", "pointers", 
    "practices",
    "string",
]
[dependencies]
